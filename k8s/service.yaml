apiVersion: v1
kind: Service
metadata:
  name: payments-api-stable
  namespace: staging
spec:
  selector:
    app: payments-api
  ports:
    - port: 80
      targetPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: payments-api-canary
  namespace: staging
spec:
  selector:
    app: payments-api
  ports:
    - port: 80
      targetPort: 8000
